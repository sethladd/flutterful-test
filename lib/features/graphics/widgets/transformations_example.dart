import 'package:flutter/material.dart';

class TransformationsExample extends StatelessWidget {
  const TransformationsExample({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Transformations Example'),
    );
  }
}
