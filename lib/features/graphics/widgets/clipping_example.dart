import 'package:flutter/material.dart';

class ClippingExample extends StatelessWidget {
  const ClippingExample({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Clipping Example'),
    );
  }
}
